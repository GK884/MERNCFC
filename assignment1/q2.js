// 2. Write the pseudocode & a program to calculate the simple interest
// based on the inputs(amount, rate, time) provided by the user.

var amount = 1000;
var rate = 2;
var time = 10;

var si = amount*rate*time;
console.log(si)